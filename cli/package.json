{
  "name": "rathercli",
  "version": "0.1.0",
  "description": "CLI for the rathernot project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^4.5.5"
  }
}
