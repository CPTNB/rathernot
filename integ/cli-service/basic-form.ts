import { provisioningRequest } from '../../cli/tst/types/SimpleAsyncForm';
import RatherNot from '../../cli/src/rathernot/index';

describe('cli-service integration', () => {
  it('should call the service with the appropriate serialized UI', () => {

  })
})
